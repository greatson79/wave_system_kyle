  `admin_api_key`,
  or set the `OPENAI_API_KEY` or `OPENAI_ADMIN_KEY`
  environment variable.

  현재 환경에는 OPENAI_API_KEY가 없고, 생성된 cut*.png 파
  일도 없습니다.

  생성 파일 목록 확인 결과:

  없음

  실행하려면 같은 쉘에서 OPENAI_API_KEY를 설정한 뒤 다시
  실행해야 합니다.

──────────────────────────────────────────────────────────
