# 적대적 성찰 보고서 — workflow.md v0.4 draft

> **상태**: 적대 성찰 / 사용자 승인 대기 / 본문 미수정
> **대상**: `prompt/workflow.md` (draft v0.4, 768줄)
> **상위 기준**: `prompt/workflow-idea/workflow-idea.md` v0.5 + PRD §0.1 제1 핵심 목적
> **출력 범위**: 본 보고서 1건 + §C 개선안 초안. workflow.md 수정 0건.
> **방식**: 적대 에이전트 A·B·C 공격 → 본문 근거 방어 → 판정 + 누적 요구 반영 감사 + 3층위 품질 검증

---

## A. 에이전트별 공격·방어·판정

### A. 운영 실패 전문가 (로컬 자원 / SOT 불일치 / CLI 단절 / 무응답 / 재귀 루프)

| # | 공격 | 방어 근거 | 판정 |
|---|---|---|---|
| A-α | **`Setup` hook 이벤트 표준 부재** — `.claude/settings.json` `hooks.Setup`은 Claude Code 표준 hook 이벤트(PreToolUse·PostToolUse·Stop·PreCompact·SessionStart·SessionEnd) 명세에 없는 사용자 정의 명칭으로 보임. Step 8 `setup_init_churchteam.py`는 `Setup` matcher `init`에 등록되는데, 이 hook이 **자동 트리거되지 않으면** Bootstrap 인프라 검증이 *실행되지 않은 채로* Step 9 부트스트랩 진입 가능 → 절대 우선순위 ②(로컬 자동 실행) 정면 훼손. 부모 게놈의 `setup_init.py`도 동일 명명을 쓰지만 *어떤 명령 진입* 시 트리거되는가가 본문에 부재. | §1 진입 게이트표·Step 8 본문 어디에도 `Setup` hook의 *트리거 조건·진입점* 명시 없음. 부모 게놈 상속 가정만 존재. | **방어 불가** |
| A-β | **Step 9 시나리오 9a `bash <(curl -fsSL ...)` ↔ 로컬 실행 불변** — curl 외부 의존 = 첫 진입 시 네트워크 필수. Step 8 검증 6항 "네트워크 연결(C-9 명시 예외 — 초기 설치 한정)"로 면책되나, 비개발자 담임목사 환경(예: 교회 사무실 네트워크 차단·외부 CDN 도달 실패)에서 0회차 진입 영구 차단. BL-5에 GUI 시나리오 9c 후보가 있으나 `bash`/`AppleScript`도 셸 종속. | Step 8 6항 "C-9 명시 예외" + BL-5 보류로 회피. 그러나 **오프라인 첫 진입 경로가 9a/9b/9c 어느 것에도 없음**. | **방어 불가** |
| A-γ | **B-1 사전 검증의 *실측 명령 자체가 검증되지 않은 가정*** — Step 6 CM-1 = `claude -p "/주간현황" --dry-run`. 그러나 `claude -p` headless 모드가 *프로젝트 디렉터리 슬래시 커맨드*를 그대로 호출하는지, `--dry-run` 플래그가 didim `/주간현황`에 정의되어 있는지 본 워크플로우 어디에도 사전 확인 부재. 검증 명령 자체가 실패하면 게이트 (iii) 통과 판정 자체 불가. | Step 6 CP-2에 "**구체 명령 1건**" 명시. 그러나 didim `/주간현황`이 `--dry-run`을 수용하는지의 *실측 결과*는 본 문서·idea 어디에도 없음. CM-1·CM-2·CM-3 중 *실측 가능 모드*가 무엇인지 사전 결정 부재 → 검증 불가능 가능성. | **방어 불가** |
| A-δ | **15개 사용자 결정 카드 일괄 응답 시 컨텍스트·세션 손실 위험** — Step 3에서 9 게이트 + 6 부수 BL = 15 카드. v0.2 P2에서 "세션 분할 권장(최대 6 카드)" + `/팀-결정 --resume`으로 회피하나, 분할 응답 도중 PRD/idea 갱신·외부 이벤트로 *결정 무효화*되면 재진입 시 어느 카드가 stale인지 판정 절차 부재. | §18 W-2(부분 응답 회기 진입)만. *stale 결정* 검증·무효화 절차 미정의. | **부분 방어 (stale 정책 부재)** |
| A-ε | **재시도 예산 RB1-RB3 표면 정의만, *예산 초과 후 영적 검토 카드 누적*의 한도 부재** — `error_handling.on_retry_budget_exceed: auto_escalate_card`. 그러나 escalate 카드 자체가 사용자 미응답 시 → 무한 누적. 자식 SOT `pastoral_decisions: []`은 무한 append 허용. 6개월 운영 시 SOT 비대 → PreCompact 부담 증가. | `retry_budgets` 스키마는 max만. *카드 누적 상한·휴면 정책*은 미정의. | **방어 불가** |
| A-ζ | **이중 SOT 페어링 — `dual_sot_pairing_guard.py`의 60초 동시 갱신 차단 매처 ↔ 정상 운용 충돌** — Step 6 (v0.3) "동일 사이클 ID 60초 윈도우" 동시 갱신 시 차단. 그러나 정상 운용 중 churchTeam이 통찰 산출 → didim sermon agent가 곧바로 status.md 갱신하는 정합 흐름이 *60초 미만*에 발생할 가능성 존재. 차단 후 사용자 `/팀-동시갱신-회수` 1동작이 *정상 운용*에서도 빈발하면 약속 1(무코드 운용) 사실상 훼손. | Step 6 알고리즘 ④ + Slash `/팀-동시갱신-회수` 회수 경로만. *오탐 빈도 추정·임계치 튜닝 절차* 부재. | **부분 방어 (오탐 정책 부재)** |
| A-η | **stale 검증의 stale** — §18 W-9 "state.yaml.last_updated 90일 초과 시 강제 재진입". 그러나 *90일 동안 last_updated를 갱신하는 주체*가 누구인지 미정. 사용자 `/팀` 호출 시 자동 갱신인지, hook 자동 갱신인지 본문 부재 → 사용자가 `/팀-건강` 1회만 호출해도 90일 카운터가 무한 연장될 수 있음. 결과: stale 검증 자체가 stale. | §18 W-9 처리 방안 1줄만. last_updated 갱신 트리거·정의 부재. | **방어 불가** |

**A 종합**: 운영 신뢰성에서 **표준 hook 미검증 / 오프라인 진입 경로 / B-1 실측 명령 자체 미검증 / 재시도 카드 누적 / dual-SOT 매처 오탐 / stale 검증 stale** 6 카테고리가 방어 불가 또는 부분 방어. v0.3에서 BL-7·BL-10 등으로 idea 측 운영 결함은 큰 폭 막았으나, **workflow.md 본문이 자체 도입한 메커니즘(`Setup` hook·CM-1 명령·60초 매처·90일 stale)에 동일 수준의 검증을 적용하지 못했다.**

---

### B. 단순성 원리주의자 (오버엔지니어링 / 과잉 추상화 / 중복 단계)

| # | 공격 | 방어 근거 | 판정 |
|---|---|---|---|
| B-α | **Step 2 4 teammate × turn 12 = 48 turn 안에 *15개 BL을 모두 후보 ≥ 2 + 결정 카드*** — gate-A(D-3+BL-5+BL-8+BL-10① = 4건), gate-B(BL-2+B-1+BL-12+BL-10④ = 4건), gate-C(BL-1+BL-13+BL-3+BL-6 = 4건), gate-D(BL-7 2건+BL-4+BL-9+BL-10②③+BL-11 = 7건). teammate-D는 12 turn 안에 7건 처리 = 한 BL당 ~1.7 turn. dense checkpoint 3종(CP-1/2/3)을 1.7 turn에 수행 = 산술적 불가능. | Step 2 turn 상한 12 + dense 명시. 그러나 BL 분배 *균형 검증* 부재. teammate-D 과부하 명시 부재. | **방어 불가 (산술 불일치)** |
| B-β | **단일 워크플로우에 12 활성 부교역자 + 14 빌드/검토 에이전트 + didim 7 위임 = ≥33 에이전트 명세** — Phase 2 종료 시. KISS 위반 + 1인 운영자 인지 부하 폭주. C-1 3-Door로 *입구는 1*이나 운영 중 *디버깅 단위*는 33+. | §17 매핑·§18 W-* 명시. 그러나 *Phase별 활성 한도* 부재. C-4 페르소나 연출이 "체험성"이라며 33인 카탈로그를 *정당화*만 하고 단순화 압박 없음. | **부분 방어 (Phase 한도 부재)** |
| B-γ | **Step 4 PRD/idea 갱신 제안 단계 자체가 v0.4 워크플로우에는 *PRD diff 작성 → 사용자 머지 → 진입 허가 카드 PRD 버전 갱신 확인*의 3홉**. v0.5 idea + v0.4 workflow + v0.6 PRD diff로 *3종 버전 동기*가 매 결정마다 발생. 메타빌드 본질을 보존하나, *비개발자가 .md diff를 검토*는 약속 1(무코드) 정신 위반. | Step 4 P1 "diff 적용 주체는 사용자" 명시. *수동 머지 = 코드*인가의 판정 본문 부재. | **부분 방어** |
| B-δ | **Hook 4종(`setup_init`·`dual_sot_pairing_guard`·`theology_filter_dual`·`output_secret_filter`) 모두 자식 인스턴스에 신규 또는 상속**. 매처 좁히기(state.yaml·output/**) v0.2에서 도입했으나 *Hook 실행 비용·동시 발화 충돌*은 검증 부재. Phase 1 진입 시 매 Edit/Write에 4 Hook 직렬 실행 = 응답 지연 누적. | Hook 매처 좁힘만. *총 실행 비용·연쇄 부담* 미평가. | **부분 방어** |
| B-ε | **`/팀-결정`·`/팀-진입허가`·`/팀-회중허가`·`/팀-건강`·`/팀-lock-회수`·`/팀-회복`·`/팀-동시갱신-회수` = 7+ 보조 슬래시** — idea §3 정체성 "단일 슬래시 진입점 `/팀`"이 사실상 7중 슬래시로 폭증. 비개발자 가독성·암기 부담. | §3 단일 진입점 약속이 본문에서 *체험적 보조 슬래시*로 확장. 명시 정당화 부재. | **방어 불가** |

**B 종합**: KISS 측면에서 **teammate-D 산술 과부하 / 33+ 에이전트 / 7+ 보조 슬래시**가 방어 불가. workflow.md가 idea 측 보존본을 *전부 실행 단계로 펼치면서* 단순화 압박을 흡수하지 못했다.

---

### C. 6개월 후 유지보수자 (모호한 단계 / 제1 목적 연결 부재 / 출처 소실 / **v0.4 헤더 약속 미이행**)

| # | 공격 | 방어 근거 | 판정 |
|---|---|---|---|
| **C-α** | **🔥 v0.4 헤더 핵심 정정 ②③④⑤ 본문 미반영** — 헤더 라인 3에 명시: ② Step 5 verification에 BL-6 위계 가시화 + Output `hierarchy-view.md` ③ Step 7 (human)에 BL-9 D-2 자동 끊김 사전 합의 카드 동봉 서명 ④ Step 12에 BL-11 외부 채널 권한 변경 결정 카드 4줄 + `/팀-권한변경` 슬래시 ⑤ §18 W-11 갱신. 본문 검증: ② Step 5 Output은 `roster.yaml + three-axis-mapping.md`만, **`hierarchy-view.md` 부재**. ③ Step 7 본문은 "(a)~(f) 5종 확인"만, **BL-9 합의 서명 절차 부재**. ④ Step 12 Verification은 "chat_id 저장 위치 명세"만, **권한 변경 4줄 카드·`/팀-권한변경` 슬래시 부재**. Slash Commands 표에 `/팀-권한변경` **미등재**. | 헤더 약속 ↔ 본문 사이의 추적성 0. v0.4 변경 이력 자체가 이 미반영을 식별하지 못함. | **방어 불가 (자체 약속 위반)** |
| C-β | **idea v0.5 §6 #26 BL-1 Done 기준 "3축 흐름 후보 ≥ 2"** — workflow.md Step 2 G4(v0.3)에서 "9종 P0 게이트 후보 ≥ 2"로 *일반화*했으나, idea §6 #26은 BL-1 *한정 ≥ 2*에 더해 **"Phase 1 *최소* 게이트 셋은 §7 권고 묶음 후속 라운드에 위임"**을 명시. workflow.md Step 10은 "신학 1차+2차+HITL+didim Wrapper+3-Door+FR-22+면책+C-4 입장 연출+§1.bis 강제+C-3 4줄"의 10+ 게이트를 Phase 1으로 *단정 채택*. → idea v0.5 위배 = "Phase 1 최소 셋 후속 라운드 위임" 약속 무시. | Step 10 verification 10+ 항목 단정. idea §6 #26 인용·해석 부재. | **방어 불가 (idea 단정 회피 약속 위반)** |
| C-γ | **Step 11 시연 주차 ID 결정을 BL-3에 추가했으나 BL-3는 "12직 명함" 라운드** — Step 11 Pre-processing "BL-3 결정 카드에 시연 주차 필드 추가". 그러나 idea §11 BL-3 Done 기준은 "12인 명함 후보안 ≥ 2 + 보고선 1장". 시연 주차 ≠ 12직 명함. 역할 혼재로 *6개월 후 maintainer가 BL-3 산출에서 시연 주차를 못 찾음*. | Step 11 본문에만 추가, idea BL-3 Done 기준 갱신 추적성 부재. | **방어 불가** |
| C-δ | **D-3 매처의 ".md 화이트리스트 키/섹션"의 *정본 위치* 부재** — Step 13 v0.2 ① 프론트매터 키 ②  본문 섹션 헤더 화이트리스트 명시. 그러나 *이 화이트리스트의 정본 파일·갱신 절차*가 본문 부재. 6개월 후 새 부교역자 추가 시 화이트리스트 업데이트가 자기 진화 게이트 통과 대상인지 자기 참조 모순. | Step 13 verification 명세만. 정본·갱신 절차 부재. | **부분 방어 (자기 참조 모순)** |
| C-ε | **`pastoral-decision-logs/` 보관 정책 자체 모순** — Runtime Directories §"`pastoral-decision-logs/`는 *commit 권장*"  ↔ Pastoral Decision Logs §`privacy_policy.storage: local_macOS_only_by_default`. 같은 문서 내 모순. 6개월 후 maintainer는 commit해야 하는지 local만 두어야 하는지 판정 불가. | 두 §에 동일 디렉터리 다른 정책 명시. 우선 순위 미명시. | **방어 불가 (자기 모순)** |
| C-ζ | **PRD↔workflow-idea↔workflow.md 3중 정합성 검증 단계 0건** — Step 1 verification은 "PRD §0.1 단일 문장 추출 + §1 게이트 9행 1:1 매핑"만. *3 문서 간 충돌 검증*(예: PRD §9.4 "자율 금지 7행" vs workflow.md Step 13 "화이트리스트 7행")이 *실제로 동일 7행인가*를 검증하는 단계 없음. 6개월 후 PRD가 v0.6/v0.7로 진화하면 workflow.md 본문이 stale인지 판정 절차 부재. | Step 1 verification 6항 + W-9 90일 stale만. *3중 정합성* 단계 부재. | **방어 불가** |
| C-η | **§17 매핑이 "미연결 항목: 없음"으로 자체 선언** — 그러나 Step 7.5(Bootstrap-of-Bootstrap)는 §17 표에 등재되었으나 idea §2 어디에도 출처 없음. workflow.md 자체 신설 = idea 보존본 위배(idea가 단정하지 않은 것을 workflow.md가 단정). PRD §11.4 인용도 §11.4 본문 검증 부재. | §17 표 Step 7.5 row 출처 = "PRD §11.4, idea A-1". A-1은 셸 1줄만, Bootstrap-of-Bootstrap 부재. | **부분 방어 (출처 부정확)** |
| C-θ | **B-2/B-5 비채택의 자기 모순** — §1.bis "B-2/B-5 Phase 1 미사용 default" + Step 10 verification "RC-β(B-2 한정) 채택 시 sermon-context.md append-only 활성". RC-β = B-2이므로 BL-2에서 RC-β가 채택되는 순간 §1.bis 위반. *RC-β 채택 시 §1.bis 자동 해제* 절차 미정의. | §1.bis ↔ Step 10 RC-β 분기 사이 정합성 명시 부재. | **방어 불가 (자기 모순)** |

**C 종합**: 유지보수성에서 **🔥 v0.4 헤더 자체 약속 4건 미반영(C-α) / idea v0.5 Phase 1 최소 셋 후속 위임 위반(C-β) / 자기 모순 3건(C-ε·C-θ + Step 13 자기 참조) / 3중 정합성 검증 부재(C-ζ)** = 본 문서가 *완벽 반영*을 자체 선언하면서 *완벽 반영하지 못한 사실 자체를 식별하지 못함*. C-α는 본 성찰의 결정타.

---

## B. 누적 요구사항 반영 감사 — workflow-idea.md v0.5 + 사용자 누적 지시

> "내가 요구한 모든 사항을 완벽하게 반영했는가"의 강한 공격. workflow-idea v0.5 본문의 누적 요구를 행 단위로 검증.

| # | 누적 요구 (출처) | workflow.md v0.4 반영 상태 | 판정 |
|---|---|---|---|
| 1 | idea §0.2 ④ "보류 항목엔 후보 ≥ 1 + 트레이드오프 1줄" | Step 2 G4로 "P0 게이트 후보 ≥ 2"는 반영. 그러나 §1.bis B-2/B-5 비채택의 *후보 형태* 부재 (Phase 2 채택 시 형태 미정) | ⚠ 부분 |
| 2 | idea §3 B-3.bis 후보안 슬롯 RC-α/β/γ | Step 6·Step 10 RC 분기로 반영 | ✅ |
| 3 | idea §6 #22 운영 신뢰성 4종 | BL-10으로 §1 부수 게이트 진입. Step 10·12·14에 산발 반영. 그러나 ② "최후 알림 채널" 후보 ≥ 2 *결정 결과*가 본문 단정 부재(Step 12 `last_resort_alert: telegram_or_macos_notification` 단정 — BL-10 ② 결정 전 단정) | ⚠ 부분 |
| 4 | idea §6 #23 didim 비커버 신규 산출진 등록 인터페이스 | BL-12로 §1 진입 게이트 (vii) 등재. 그러나 Step 5/6/10 어디에도 *등록 인터페이스 운용 단계* 명세 부재 | ⚠ 부분 |
| 5 | idea §6 #24 결정 동선 비개발자 UX 형식 | Step 3 4줄 카드 + 부담 분산 정책으로 반영 | ✅ |
| 6 | idea §6 #25 BL Done 기준·결과 산출물·닫힘 신호 7열 | state.yaml `entry_gates` 3필드 추가(G5)로 반영 | ✅ |
| 7 | idea §6 #26 Phase 1 *최소* 게이트 셋 후속 위임 | **Step 10이 10+ 게이트 단정 → 위반** (C-β 참조) | ❌ |
| 8 | idea §11 BL-1 Done "3축 흐름 후보 ≥ 2" | Step 2 gate-C CP-1로 반영 | ✅ |
| 9 | idea §11 BL-12 등록 인터페이스 후보 ≥ 2 | Step 2 gate-B에 위임 표기만, *후보 ≥ 2 명시* 부재 | ⚠ 부분 |
| 10 | idea §11 BL-13 FR-22 6종 PRD 인용 6항 일치 | Step 2 gate-C CP-2 "후보 ≥ 1"만 — idea Done 기준은 "PRD 인용 6항 일치 검증" + "한국어 라벨 후보 ≥ 1". *6항 일치 검증* 명시 누락 | ⚠ 부분 |
| 11 | idea §7 R-3 C-4 신학 면책 안전판 3종 | Step 10·15에 반영 | ✅ |
| 12 | idea §7 R-5 B-2/B-5 Phase 1 미사용 | §1.bis로 반영. 단 RC-β 채택 시 자기 모순 (C-θ) | ⚠ 부분 |
| 13 | **v0.4 헤더 핵심 정정 ②③④⑤** | **본문 4건 모두 미반영 (C-α 결정타)** | ❌ |
| 14 | PRD §0.1.bis 약속 1 무코드 | Step 4 머지 주체·Step 7.5 안내선·Slash Commands 7+ → 약속 1 흔들 (B-ε) | ⚠ 부분 |
| 15 | PRD §11.5 진실 페어링 | dual_sot_pairing_guard 알고리즘 + 60초 매처. 단 오탐 정책 부재 (A-ζ) | ⚠ 부분 |
| 16 | 절대 우선순위 ② 로컬 자동 실행 | Step 9 9a curl 의존 + `Setup` hook 트리거 미검증 (A-α/A-β) | ❌ |
| 17 | 절대 우선순위 ③ 목회자 최종 검토 | Step 7·11·15 + D-2 카드. 단 BL-9 합의 서명 절차 부재 (C-α ③) | ⚠ 부분 |

**B 종합**: 누적 요구 17건 중 **완전 반영 5건 / 부분 반영 9건 / 미반영 3건**. 미반영 3건이 모두 *결정타급*: idea §6 #26 위반 / **v0.4 헤더 자체 약속 위반** / 절대 우선순위 ② 훼손. "완벽 반영" 자체 선언은 사실과 다름.

---

## 3층위 품질 검증

**1층위 (사실 확인 — 방어 불가 항목만)**:
- A-α `Setup` hook 트리거 미검증
- A-β 9a curl 의존 = 오프라인 진입 불가
- A-γ B-1 실측 명령 자체 미검증
- A-ε 재시도 카드 누적 상한 부재
- A-η stale 검증 stale (90일 카운터 갱신 주체 미정)
- B-α teammate-D 산술 과부하 (12 turn 7 BL)
- B-ε 7+ 보조 슬래시 (단일 진입점 위반)
- **C-α v0.4 헤더 정정 ②③④⑤ 본문 미반영**
- C-β idea v0.5 §6 #26 Phase 1 최소 셋 후속 위임 위반
- C-γ Step 11 시연 주차 ↔ BL-3 역할 혼재
- C-ε pastoral-decision-logs 보관 정책 자기 모순 (commit 권장 ↔ local-only)
- C-ζ PRD↔idea↔workflow 3중 정합성 검증 단계 0건
- C-θ §1.bis ↔ Step 10 RC-β 자기 모순

**2층위 (구조 분석 — 실제 실행 블로커)**:
- C-α는 *v0.4 헤더 자체가 약속한 것을 본문이 안 한* 추적성 붕괴 → 어떤 자동화도 헤더 약속을 신뢰할 수 없음. 가장 큰 실행 블로커.
- A-α/A-β/A-γ 결합: Bootstrap 자체가 *3중 미검증 가정* 위에 서 있음 → Phase 0 통과 자체가 불확실.
- B-α: Step 2가 *완료될 수 없는 산술 구조* → 진입 게이트 9종 해소 게이트가 일정 안에 닫히지 않음 → Implementation 영구 봉인.
- C-ε: 자기 모순은 운영 시점에 *어느 정책을 따라야 하는지*의 1동작 결정 불가 → SPOF.

**3층위 (제1 목적 정합성)**:
- 절대 우선순위 ② "로컬 자동 실행 불변" — A-α/A-β로 정면 훼손. Bootstrap이 표준 hook 미검증 + curl 의존 = "1인 운영자 로컬 macOS"의 사역 자율성 깨짐.
- 절대 우선순위 ③ "목회자 최종 검토" — C-α ③(BL-9 합의 서명 부재)로 *D-2 자동 끊김 사실의 사용자 사전 합의 자리* 누락 → 사용자가 D-2 카드의 *설계 사실* 모르고 진입 가능.
- 약속 1 무코드 — B-ε 7+ 슬래시 + Step 4 사용자 git apply diff 머지 = 비개발자 인지 부하 폭증.

**3층위 종합**: workflow.md v0.4는 idea v0.5의 *원칙 보존*은 강하지만, *실행 경로 자체가 자기 모순·자기 약속 위반·미검증 가정* 위에 서 있다. **제1 핵심 목적의 ③ 다매체 실행 보조** 흐름이 Phase 0 부트스트랩 단계에서 끊길 가능성이 산술적으로 식별된다(B-α + A-γ).

---

## C. 개선안 초안 (사용자 승인 대기 — workflow.md 본문 미수정)

> 본 절은 *권고 형태*. 사용자 승인 후에만 workflow.md 본문 수정 진행. 각 항목 우선순위 P0/P1.

### P0 (실행 진입 자체 차단 — 즉시 보강)

**P0-1 [C-α 결정타 해소]** v0.4 헤더 핵심 정정 ②③④⑤ 본문 외과적 반영:
- ② Step 5 Output에 `hierarchy-view.md` 추가 + Verification에 BL-6 채택안 1줄 의무
- ③ Step 7 (human) Action에 BL-9 D-2 자동 끊김 사전 합의 카드 서명(0회차 1회) 절차 명시 + `/팀-합의` 또는 `/팀-진입허가` 카드 안에 합의 항목 통합
- ④ Step 12 Verification에 BL-11 권한 변경 결정 카드 4줄 명세 추가 + Slash Commands 표에 `/팀-권한변경` 등재
- ⑤ §18 W-11/W-12 미반영 idea 4건 추적성 행 정합성 재확인

**P0-2 [A-α/A-β/A-γ 결합 — Bootstrap 미검증 가정 차단]**
- `Setup` hook이 표준이 아니면 Step 8을 SessionStart matcher 또는 명시 슬래시 `/팀-점검`로 재정의
- Step 9 9a `bash <(curl ...)` 분기를 *오프라인 사전 다운로드 전제*로 변경 + 9c GUI 시나리오에 *오프라인 첫 진입 후보* 형태 ≥ 1 추가
- Step 6 CM-1 `claude -p "/주간현황" --dry-run` 자체의 *실측 가능성* 사전 확인 단계 Step 5.5 신설(Phase 0 진입 전 1회)

**P0-3 [C-β idea v0.5 §6 #26 위반 해소]** Step 10 verification의 10+ 게이트 단정을 *BL-1 산출 결합 후속 라운드에 위임* 표현으로 약화. v0.3 G3가 D-3에 적용한 패턴을 D-1/D-2/HITL/didim Wrapper에도 확장.

**P0-4 [B-α teammate-D 산술 과부하]** Step 2 BL 분배 재균형:
- gate-D 7건을 6건 이하로 분배 (BL-11을 gate-C로 이관 또는 부수 BL로 강등)
- turn 상한 12 → 18로 상향 또는 BL당 *최소 turn 보장*(예: ≥ 2) 명시

**P0-5 [C-ε 자기 모순]** Runtime Directories `pastoral-decision-logs/` "commit 권장" 라인을 삭제하고 Pastoral Decision Logs §`privacy_policy`로 단일 출처화.

**P0-6 [C-θ 자기 모순]** §1.bis 라인에 "RC-β 채택 시 본 미사용 default 자동 해제 — Phase 1 *내* 채택 카드 1회 통과로만" 단서 추가.

### P1 (운영 신뢰성·유지보수 — 다음 라운드)

**P1-1 [A-ζ dual-SOT 60초 매처 오탐]** 60초 윈도우의 *오탐 임계치 튜닝 절차* + 정상 운용 동시 갱신 화이트리스트(예: `didim_pointer.last_seen` 갱신은 차단 제외) 정의.

**P1-2 [A-η stale 검증의 stale]** `state.yaml.last_updated` 갱신 트리거를 *Team Lead SOT 쓰기 시점*으로 한정. `/팀-건강` 호출은 *읽기 전용*이므로 갱신 미발생을 명시.

**P1-3 [A-ε 재시도 카드 누적]** `pastoral_decisions: []` 무한 append 차단 — 30일 미응답 카드 자동 휴면 + `/팀-건강`에 휴면 카드 카운터 노출.

**P1-4 [B-ε 보조 슬래시 폭증]** 7+ 슬래시를 `/팀 [동사]` 단일 진입점 + 한국어 인자(`/팀 결정`·`/팀 회복`)로 통합 또는 3-Door 안에 결정/회복/권한 동선 흡수.

**P1-5 [C-γ Step 11 시연 주차]** 시연 주차 ID 결정을 *BL-3가 아닌 Step 11 자체 진입 게이트*로 분리.

**P1-6 [C-ζ 3중 정합성 검증]** Step 1 verification에 "PRD §X ↔ idea §Y ↔ workflow.md §Z 3행 인용 정합 검증" 명시 + 90일 stale 검증과 별개 사이클 신설.

**P1-7 [C-δ D-3 화이트리스트 정본]** 화이트리스트 키/섹션 정본 파일 경로 명시(예: `Claude_skills/AI_churchteam/.claude/self-evolution-whitelist.yaml`) + 갱신 절차 = 자기 진화 게이트 자기 통과 의무.

**P1-8 [C-η §17 출처 부정확]** Step 7.5 출처를 idea A-1이 아닌 *workflow.md 자체 신설*로 정정. idea 미보존 항목임을 명시 + 차후 idea v0.6 라운드에서 보존본 추가 안건 등록.

### 미반영 (idea 후속 라운드 위임)

- A-δ stale 결정 무효화 정책
- B-β Phase별 활성 한도
- B-γ 사용자 머지 = 코드 판정

---

## 승인 요청 (구현 의도)

**구현 의도 1줄**: P0-1~P0-6 6건을 workflow.md v0.4 → v0.5 외과적 reflection 반영 (본 보고서가 식별한 자기 약속 위반·자기 모순·산술 불일치 차단).

**영향 파일**: `prompt/workflow.md` 단일. idea/PRD 무수정. (P0-2의 hook 명세 변경은 자식 인스턴스 코드 미작성 단계이므로 설계도만 갱신.)

**위험 지점**:
1. P0-3(idea §6 #26 반영 = Step 10 약화)이 Phase 1 시연 가능성을 *추가 후속 라운드까지 보류*시킴 → 사용자 일정 압박.
2. P0-4(BL 분배 재균형)이 idea §11 BL 우선순위를 흔들 수 있음 → idea v0.6 동시 갱신 필요할 가능성.
3. P0-2(`Setup` hook 재정의)가 부모 게놈 hook 패턴과 분기 → 부모 게놈 정합성 재검증 필요.

**가정 사항**:
- v0.4 헤더의 정정 ②③④⑤는 *반드시 반영되어야 할 약속*이라는 사용자 의지 (본 성찰의 핵심 가정).
- idea v0.5 §6 #26 "Phase 1 최소 셋 후속 위임"이 사용자 누적 요구와 정합 (충돌 시 사용자가 어느 쪽을 우선할지 결정 필요).
- workflow.md를 v0.5로 진입시킬지, 본 성찰을 *추가 적대적 라운드*로 누적할지의 사용자 결정.

— end of REFLECTION-workflow-v0.4-adversarial.md —
