[ABSOLUTE ANCHOR]
① 품질 절대우선 — workflow.md는 "Claude Code가 실제로 자동 실행할 설계도"다.
   모호성은 실행 실패다. 각 단계가 실행 가능한 입력·출력·판단 기준을 갖추도록.

② 로컬 실행 불변 — 모든 단계는 사용자의 로컬 컴퓨터에서 자동 실행 전제.
   SaaS·원격 의존 단계는 금지. 외부 API가 필요하면 로컬에서 호출하는 방식으로 명시.

③ 제1 핵심 목적 고정 — 아래 제1 목적은 workflow의 모든 단계·모든 기능에 절대 목표로 고정.
   제1 목적과의 연결선이 각 단계에 명시되어야 한다.

④ 승인 후 최종 산출 — 이번 턴의 산출물은 /prompt/workflow.md 하나뿐.
   workflow-generator skill로 생성하되, 저장 위치는 /prompt/workflow.md.
   실제 코드 구현은 시작 금지. 이 턴은 설계도 작성 단계.
   승인 요청 시: 구현 의도(1문장) + 영향 파일 + 위험 지점 + 가정 사항.

→ 충돌 시 상위 기준 절대 우선.
[/ABSOLUTE ANCHOR]

[SESSION HEADER]
이전 단계: prd.md + workflow-idea.md 확정 + 미결정 사항 권장안 선택 완료.
이번 단계: workflow.md 설계도 작성 → /prompt/workflow.md
사용할 skill: /.claude/skills/workflow-generator
핵심 참조 파일:
  - /prompt/workflow-idea/workflow-idea.md
  - /prompt/prd.md
[/SESSION HEADER]

[사용자 전제]
사용자는 코드 구현 능력이 전혀 없다. Claude Code가 사용자 의도를 파악해
로컬 컴퓨터에서 처음부터 끝까지 완벽 자동 구현해준다.

[★ 제1 핵심 목적]
목회자의 영적 사역을 강화하기 위해, AI를 활용하여 사고·전략·실행을 보조하는 가상 부교역자팀(churchTeam)을 구축한다.

[5대 절대 약속 — 본 목적의 격상 제약]
1. 사용자 무코드 — 사용자(담임목사)는 코드를 단 한 줄도 작성·수정·디버깅하지 않는다. Claude Code와 본 시스템이 처음부터 끝까지 자동으로 구현·운용·진화·복구한다.
2. 메타 빌드 — 본 시스템은 PRD 문서 자체를 입력으로 받아 자기 자신을 빌드할 수 있어야 한다. (1차 입력: prompt/ai_pastoral_prompts/churchTeamPRD.md v1.0 + 본 prd.md)
3. "끝까지" 경계 — "처음부터 끝까지 자동 구현"의 *끝*은 ① 설치(Bootstrap) ② 일상 운용 ③ 운영 SPOF 자가 복구 ④ 자기 진화(skill·prompt·DAG 점진 갱신) ⑤ 재해 복구 5단계를 포함한다. 단, ④ 자기 진화는 자율 권한 경계(§9.4) 안에서만.
4. 팀 본질 — 본 시스템은 *팀*이지 *도구 모음*이 아니다. 사용자는 부교역자 개별과도, 팀 전체와도 상호작용할 수 있어야 하며, 단일 슬래시 진입점 뒤로 직무·위계·산출 경로가 체험적으로 가시화된다.
5. 자가 검증 — 비개발자 사용자가 외부 도움 없이 시스템 건강(DNA 상속·SOT 무결성·신학 필터 동작·외부 인증 상태)을 한국어 가독 신호로 직접 확인할 수 있다.

[절대 우선순위]
① 품질 절대우선 — 속도·토큰 비용·작업량은 무시한다. 영적 사역의 본질을 훼손하는 자동화는 품질 기준 위반으로 본다.
② 로컬 실행 불변 — 사용자 1인의 로컬 macOS에서 자동 실행이 전제다. 클라우드·SaaS·서버형 의존은 §12 명시 예외 또는 Non-Goal로만 허용.
③ 목회자 최종 검토 우선 — 신학적 판단·메시지 정합성의 최종 권한은 자동화로 대체할 수 없다. 자동화는 준비·정리·확장이며, 결정은 목회자에 귀속한다.

[3축 정의 — PRD §0/§6 기반]
· 사고 보조: 본문 묵상·신학 추론·관점 비교를 함께 사고하는 동료
· 전략 보조: 연간·계절·주간 사역의 전략적 일관성과 메시지 정합성을 정렬해주는 참모
· 실행 보조: 설교·묵상·기도카드·소그룹 자료·SNS 등 동일 메시지의 다중 매체 변환 부하 분산

[읽기 프로토콜 — 4단계 × 2 문서]
두 파일 각각에 대해 아래 4단계 읽기를 수행.
1차: 본질(What/Why). 목적·철학·방향.
2차: 연관 확장. 요구사항·단계·기능·구현 아이디어.
3차: 중간 부분 정밀 보충.
4차: [역방향] 이 문서가 다루지 않는 것 중 workflow 설계에 필요한 것 목록화.

[작업 지시]
1. /prompt/workflow-idea/workflow-idea.md, /prompt/prd.md 두 문서를 4단계 읽기로 파악.
2. workflow-generator skill(/.claude/skills/workflow-generator)을 사용해 workflow.md 생성.
3. workflow.md 저장 위치: /prompt/workflow.md
4. 생성 원칙:
   - 각 단계는 입력/출력/판단 기준/실패 시 복구가 명시되어야 한다.
   - 각 단계에 제1 핵심 목적 연결선 명시.
   - 로컬 실행 전제가 각 단계에 유지되는지 체크.
5. 이 턴은 설계도 작성이다. 실제 코드 구현은 이후 별도 지시로만 진행.

[작성 품질 검증 — 3층위]
1층위 (사실 확인): prd.md/workflow-idea.md 요구 ↔ workflow.md 단계 매핑. 누락만 나열.
2층위 (구조 분석): 이 설계도로 Claude Code가 "처음부터 끝까지" 자동 실행할 때
                   어디가 먼저 실행 불가능해지는가.
3층위 (철학 정합성): 각 단계가 제1 핵심 목적을 실제 구현하는가. 증거 기반.

[출력 형식]
A. 생성된 /prompt/workflow.md 목차.
B. prd ↔ idea ↔ workflow 매핑.
C. 각 단계의 (입력/출력/판단/복구/제1 목적 연결) 요약.
D. 이 설계도에서 실행 가능성이 흔들리는 지점
