{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wave.ai/harness/sprint_contract.schema.json",
  "title": "Sprint Contract (structured sidecar to sprint_N_contract.md)",
  "description": "The negotiated contract between Generator and Evaluator. Generator proposes; Evaluator reviews/augments. Must carry >=15 granular, Playwright-testable criteria.",
  "type": "object",
  "additionalProperties": false,
  "required": ["sprint", "features", "criteria", "definition_of_done", "status"],
  "properties": {
    "sprint": { "type": "integer", "minimum": 1 },
    "name": { "type": "string" },
    "features": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" },
      "description": "Specific features implemented in this sprint"
    },
    "tech_decisions": {
      "type": "array",
      "items": { "type": "string" }
    },
    "criteria": {
      "type": "array",
      "minItems": 15,
      "description": "Granular, individually testable acceptance criteria. Minimum 15 enforced by the Evaluator.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "description", "testable_via"],
        "properties": {
          "id": { "type": "integer", "minimum": 1 },
          "description": {
            "type": "string",
            "minLength": 8,
            "description": "Concrete, verifiable statement — NOT vague ('looks good')."
          },
          "testable_via": {
            "type": "string",
            "enum": ["playwright", "http", "db", "bash", "visual"],
            "description": "How the Evaluator will verify it"
          }
        }
      }
    },
    "definition_of_done": {
      "type": "string",
      "minLength": 10
    },
    "self_evaluation": {
      "type": "object",
      "description": "Generator's honest self-check before handoff (filled at READY_FOR_QA).",
      "additionalProperties": false,
      "properties": {
        "criteria_self_passed": { "type": "integer", "minimum": 0 },
        "known_gaps": {
          "type": "array",
          "items": { "type": "string" }
        },
        "app_start_command": { "type": "string" },
        "app_url": { "type": "string" }
      }
    },
    "status": {
      "type": "string",
      "enum": ["proposed", "approved", "building", "ready_for_qa"],
      "description": "Mirrors the signal token currently at the bottom of the .md file"
    }
  }
}
